# Matrix app — workspace kit

Chat-first workspace. Sidebar + chat + canvas in a three-column grid.

Mounts the real Vestaboard renderer (`mini-vestaboard`) inside the chat artifact and the canvas rail — the board is the product, not a placeholder.

Boot: `import { bootDesignSystemPage } from '../../system/registry.js'`.
